# =========================================
# ROUTING PRINCIPAL - APPLICATION SYMFONY
# =========================================

# Import automatique des controllers avec attributes
controllers:
    resource: ../src/Controller/
    type: attribute

# =========================================
# AUTH / LOGIN API (JWT ou autre auth)
# =========================================
api_login_check:
    path: /api/login_check
